#!/usr/bin/env python3
"""CORD command mediator — route commands through SENTINEL's CORD engine.

Usage:
    cord git push origin main
    cord rm -rf ~/Downloads/junk
    cord --lock
    cord --status
    cord --log
    cord --verify
"""
import sys
import os

# Ensure cord_engine is importable
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from cord_engine.cli import main
main()
